# Source

Canonical source for real FROSTMOURNE game components belongs here.

There are currently no game modules. Add a component only when actual source exists, then register it in `AI_INDEX.json` and `runtime/current.json` after a real x86 build exists.
